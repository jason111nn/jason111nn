name: generate profile visual contents

on:
  schedule:
    - cron: "0 */12 * * *" # 每 12 小時更新一次
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write 

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10 # 稍微增加時間，因為要跑兩組圖表
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create dist directory
        run: mkdir -p dist

      # 產生 貪食蛇 貢獻圖
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: jason111nn
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      # 產生 GitHub 統計數據圖
      - name: generate github-stats.svg
        uses: teoxoy/github-stats-action@v1.3.1
        with:
          token: ${{ secrets.GH_STATS_TOKEN }}
          filename: dist/github-stats.svg 
          theme: dracula 

      # 將所有產生的 svg 推送到 output 分支
      - name: push all contents to the output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}